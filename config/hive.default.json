{
  "version": 3,
  "runtime": {
    "name": "HIVE",
    "shared_memory": "comb",
    "bus": "waggle",
    "worker_defaults": "workers.default.json",
    "experiment_templates": "experiment_templates.json"
  },
  "notes": [
    "Workers are complete experiment configurations.",
    "JEV and LLM are independently toggleable per worker.",
    "Worker-to-worker routing is planned and is not executed by this file."
  ]
}
